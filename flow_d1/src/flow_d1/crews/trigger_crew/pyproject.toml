[project]
name = "trigger_crew"
version = "0.1.0"
description = "trigger_crew using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]>=0.177.0,<1.0.0"
]

[project.scripts]
trigger_crew = "trigger_crew.main:run"
run_crew = "trigger_crew.main:run"
train = "trigger_crew.main:train"
replay = "trigger_crew.main:replay"
test = "trigger_crew.main:test"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
